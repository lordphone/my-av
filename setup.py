from setuptools import setup, find_packages

setup(
    name='my-av',
    version='0.1.0',
    author='Lordphone Wen',
    packages=find_packages(),
    install_requires=[
        # conda is managing this
    ],
)